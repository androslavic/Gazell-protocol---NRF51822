#ifndef SENDDATA_H__
#define SENDDATA_H__

#include "uart.h"
#include "gzll.h"
#include <stdarg.h>

#include <stdarg.h>
#include <stdio.h>


#endif 

int sendData(const char *format, ...);

int terminalIn(const char *format, ...);
int terminalOut(const char *format, ...);

